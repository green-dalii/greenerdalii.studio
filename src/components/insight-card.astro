---
import type { InsightSite } from "../types";
import { Image } from "astro:assets";
import { blogImages } from "~/content/config";
export interface Props {
  site: InsightSite;
}

const { post } = Astro.props;
---

<!-- Card -->
<a class="group relative block" href={`/insight/${post.slug}/`}>
  <div
    class="relative h-[350px] w-full flex-shrink-0 overflow-hidden rounded-xl before:absolute before:inset-x-0 before:z-[1] before:h-full before:w-full before:bg-gradient-to-t before:from-gray-900/[.7] before:group-hover:bg-gradient-to-t"
  >
    <Image
      class="absolute left-0 top-0 h-full w-full object-cover transition-all duration-300 group-hover:scale-110 group-hover:shadow-2xl"
      src={blogImages[post.data.heroImage]?.default}
      alt="Album"
    />
  </div>

  <div class="absolute inset-x-0 bottom-0 z-10">
    <div class="flex h-full flex-col p-4 sm:p-6">
      <h3
        class="font-semibold text-white text-lg group-hover:text-white/[.8] sm:text-xl"
      >
        {post.data.title}
      </h3>
      <p class="mt-2 text-white/[.8] text-xs">
        {post.data.description}
      </p>
    </div>
  </div>
</a>
<!-- End Card -->